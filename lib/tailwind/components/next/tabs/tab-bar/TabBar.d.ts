import * as React from "react";
import { ReactNode } from "react";
/**
 * Tab Bar component acts as a styling wrapper (and tablist role) for tabs
 */
export declare const TabBar: ({ children, className, }: {
    children: ReactNode;
    className?: string;
}) => React.JSX.Element;
